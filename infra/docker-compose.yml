services:
  db:
    image: postgres:16
    container_name: rentbot-postgres
    environment:
      POSTGRES_USER: rentbot
      POSTGRES_PASSWORD: rentbot
      POSTGRES_DB: rentbot
    ports:
      - "15432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
